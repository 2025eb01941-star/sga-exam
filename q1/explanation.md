# Q1 Explanation

- The `whoami` and `groups` commands confirmed the active Linux user and the permissions groups associated with that account.
- The shell and current working directory were verified to ensure the session was operating in the expected environment.
- The directory listing showed that the current workspace folder was empty at the time of the check.
- The network connectivity test confirmed that outbound access to the target site was working.
